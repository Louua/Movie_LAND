import React from "react";
//3d1189de
import ReactDOM from "react-dom"; // Corrected import
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
